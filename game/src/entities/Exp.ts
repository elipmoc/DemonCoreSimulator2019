
export class Exp extends g.Sprite {
    constructor(scene: g.Scene, src: g.ImageAsset) {
        super({
            scene,
            src,
            width: src.width,
            height: src.height,
        });
    }
}